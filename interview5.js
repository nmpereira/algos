/*
input: heads of 2 sorted linked lists
merge 2 lists together into 1 sorted list

new list => splicing both input linked lists

 return head of the merged linked list

*/

// [[1]->[2]->[3]->null]
// [[3]->[4]->[5]->null]
// // [[1]->[2]->[3]->[3]->[4]->[5]->null]

/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} list1
 * @param {ListNode} list2
 * @return {ListNode}
 */

let array = [ 'a', 'b', 'c', 'd' ];

array.filter((e) => array.push(e));
